--- a/omicverse/utils/agent_backend.py
+++ b/omicverse/utils/agent_backend.py
@@ -328,9 +328,9 @@ class LLMBackend:
             system_prompt = kwargs.pop("system_prompt", "")
             user_prompt = kwargs.pop("user_prompt", "")

-        if len(user_prompt) > 100000:
+        if len(user_prompt) > 200000:
             raise ValueError(
-                f"user_prompt too long ({len(user_prompt)} chars, max 100000)"
+                f"user_prompt too long ({len(user_prompt)} chars, max 200000)"
             )

         # GPT-5 series uses Responses API with different parameter format
@@ -370,9 +370,9 @@ class LLMBackend:
             system_prompt = kwargs.pop("system_prompt", "")
             user_prompt = kwargs.pop("user_prompt", "")

-        if len(user_prompt) > 100000:
+        if len(user_prompt) > 200000:
             raise ValueError(
-                f"user_prompt too long ({len(user_prompt)} chars, max 100000)"
+                f"user_prompt too long ({len(user_prompt)} chars, max 200000)"
             )

         headers = {"Authorization": f"Bearer {self.api_key}"}
